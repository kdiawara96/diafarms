package com.diafarms.ml.enums;

// D'où vient le paiement — purement descriptif, l'argent est le même partout.
public enum OriginePaiement {
    ACOMPTE, LIVRAISON, VENTE, REGLEMENT, FACTURE, REPRISE
}
