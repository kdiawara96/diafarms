package com.diafarms.ml.enums;

// Types de stock qu'un magasin de vente peut recevoir par transfert — volontairement
// limité à œufs/réforme (les seuls types explicitement demandés avec un vrai stock par
// magasin) ; les fientes restent une vente farm-wide simple, sans stock ni magasin.
public enum TypeStockMagasin {
    OEUFS,
    REFORME
}
